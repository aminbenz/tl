import { MarketingConfig } from "types"

export const marketingConfig: MarketingConfig = {
  mainNav: [
    {
      title: "Dashboard",
      href: "/admin/dashboard",
    },
    {
      title: "Contact",
      href: "/contact",
      disabled: true,
    },
  ],
}
